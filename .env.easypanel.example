# =============================================================================
# LBM Advogados - Variáveis para EasyPanel
# =============================================================================
# Banco e MinIO já existem no EasyPanel. Use os hosts/nomes internos dos
# serviços (ex: postgres, minio) ou as URLs que o EasyPanel fornece.
#
# BACKEND: configure estas variáveis como "Environment" no app do backend.
# FRONTEND: só precisa de VITE_API_URL como "Build argument" (build-time).
# =============================================================================

# --- BACKEND (runtime) ---

# Postgres - use o host do seu Postgres no EasyPanel (ex: nome do serviço ou URL)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# MinIO - use o host do MinIO no EasyPanel
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=seu_access_key
MINIO_SECRET_KEY=sua_secret_key
MINIO_BUCKET=lbm-documents
MINIO_USE_SSL=false

# JWT - gere um segredo forte para produção
JWT_SECRET=seu-jwt-secret-super-seguro-altere-em-producao

# API
PORT=3000
NODE_ENV=production

# CORS - URL(s) do frontend que podem chamar a API. Vários: separados por vírgula.
CORS_ORIGIN=https://app.seudominio.com

# --- FRONTEND (build-time only) ---
# Defina como Build Arg no EasyPanel (não como env de runtime).
# URL base da API, incluindo /api (ex: https://backend.seudominio.com/api).
VITE_API_URL=https://backend.seudominio.com/api
